services:
  kilo:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
